{
  "name": "upasana-portfolio",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview --port 3000",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "astro": "^4.0.0"
  }
}
